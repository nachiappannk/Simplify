<UserControl x:Class="SimplifyUi.DisplayMessagesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimplifyUi"
             xmlns:viewModel="clr-namespace:SimplifyUi.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel:DisplayMessagesViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Title}" Height="40" 
               FontSize="20" Background="Blue" Foreground="White" 
               FontWeight="Bold" HorizontalAlignment="Stretch"/>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Messages}"/>
        <DockPanel Grid.Row="2"  LastChildFill="False" Height="40" Background="AliceBlue" HorizontalAlignment="Stretch">
            <Button Content="Next" Width="60" Margin="6" DockPanel.Dock="Right" Command="{Binding NextStepCommand}"/>
        </DockPanel>
    </Grid>
</UserControl>
